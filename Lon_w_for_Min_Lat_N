SET NOCOUNT ON;
/*Enter your query here.
Please append a semicolon ";" at the end of the query and enter your query in a single line to avoid error.
*/
declare @a float
set @a = (select min(lat_n) from station where lat_n>38.7880 )

select cast(round(long_w,4) as decimal(10,4)) from station where lat_n =@a 
GO 
